// substr => returns a part of a string between the start index and a number
// of characters after it.

// it extracts length of characters from a string counting from start index

let str = 'Radio, live transmission. Listen to the silence, let it ring on. Eyes, dark grey lenses frightened of the sun.'

console.log((str.substr(1,5)))

// o/p = adio,
// first argument = start index
// second argument = no of characters to extract